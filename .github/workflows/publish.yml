name: Publish to VS Code Marketplace

on:
  push:
    tags:
      - 'v*' # Se déclenche uniquement quand on pousse un tag, ex: git tag v0.1.1 && git push --tags

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20.x
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run Tests
        run: xvfb-run -a npm test

  publish:
    name: Publish Extension
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20.x
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Compile Extension
        run: npm run compile

      - name: Publish to VS Code Marketplace
        run: npx @vscode/vsce publish --pre-release --pat ${{ secrets.VSCE_PAT }}
